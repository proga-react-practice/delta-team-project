const textPattern = /^[A-Z][a-z]*$/;
const numberPattern = /^\+38\(0\d{2}\) \d{3} \d{4}$/;
const emailPattern = /.+@.+/;

const format = "YYYY-MM-DD HH:mm";

export { textPattern, numberPattern, emailPattern, format }